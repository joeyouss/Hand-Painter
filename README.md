# Hand-Painter
Paint with your hands using your webcam. Extended finger draws, pinch resizes the brush, pinky-only clears the canvas. Runs in the browser — single HTML file, no install needed. Built with MediaPipe Hands + Canvas API.
